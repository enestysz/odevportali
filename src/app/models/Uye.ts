import { Value } from "firebase/remote-config";

export interface Uye {
  uid: string;
  email?: string;
  displayName?: string;
  tel?: string;
  adres?: string;
  foto?: string;
  admin?: number;
}
